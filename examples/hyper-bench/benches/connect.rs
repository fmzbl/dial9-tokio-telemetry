#![feature(test)]

extern crate test;

// Placeholder, mirrors hyper/benches/connect.rs (which is empty pending a
// rewrite against hyper::client::conn). Kept so the bench file set matches
// hyper 1:1.
